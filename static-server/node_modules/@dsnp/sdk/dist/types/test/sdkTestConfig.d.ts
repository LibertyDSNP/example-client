import { Signer, providers } from "ethers";
declare type SdkTestConfig = {
    provider: providers.JsonRpcProvider;
    signer: Signer;
};
export declare const setupConfig: () => SdkTestConfig;
export {};
