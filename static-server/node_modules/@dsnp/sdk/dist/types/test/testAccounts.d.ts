import { ContractTransaction, ethers } from "ethers";
import { Registration } from "../core/contracts/registry";
import { DSNPUserId } from "../core/utilities/identifiers";
export interface RegistrationWithSigner extends Registration {
    signer: ethers.Signer;
}
export declare const getSignerForAccount: (accountIndex: number) => ethers.Signer;
export declare const getIdFromRegisterTransaction: (transaction: ContractTransaction) => Promise<DSNPUserId>;
export declare const newRegistrationForAccountIndex: (acctIdx: number, handle: string) => Promise<RegistrationWithSigner>;
