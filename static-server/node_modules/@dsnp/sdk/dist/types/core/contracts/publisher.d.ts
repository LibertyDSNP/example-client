import { ContractTransaction, EventFilter } from "ethers";
import { HexString } from "../../types/Strings";
export interface Publication {
    dsnpType: number;
    url: string;
    hash: HexString;
}
export declare const publish: (publications: Publication[]) => Promise<ContractTransaction>;
export declare const dsnpBatchFilter: () => Promise<EventFilter>;
